apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ctrl-eat-api-pv-claim
  labels:
    app: ctrl-eat-api
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
